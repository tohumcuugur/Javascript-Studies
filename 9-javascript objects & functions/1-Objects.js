let val;

let num = 10;
//object literal
let ugur = {
    name: 'ugur',
    yearOfBirth:1996,
    job: 'Front End Developer'
}
//array. array is an object too. So we can use object and array features with "array".
let numbers = [1, 2, 3, 4]
val = ugur;
val = numbers;


console.log(val);
console.log(typeof val);